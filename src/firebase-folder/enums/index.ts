export enum FirebaseAuthFormItemKeyType {
  email = 'email',
  password = 'password',
}

export enum FirebaseNewRestaurantFormItemKeyType {
  hidden = 'hidden',
  name = 'name',
  imageUrl = 'image-url',
}

export enum FirebaseNewOrderFormItemKeyType {
  itemName = 'item-name',
  cost = 'cost',
  userReference = 'user-reference',
}

export enum FirebaseNewMenuImageFormItemKeyType {
  name = 'name',
  file = 'file',
}

export enum FirebaseNewRestaurantMenuItemFormItemKeyType {
  name = 'name',
  cost = 'cost',
  type = 'type',
  hidden = 'hidden',
}
